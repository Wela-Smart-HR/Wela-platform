# 🏗️ กฎเหล็กโครงสร้างโปรเจกต์ Smart-HR (Architecture Rulebook)

เอกสารนี้คือกฎมาตรฐานสำหรับการเขียนโค้ดในโปรเจกต์ Smart-HR เพื่อให้โค้ดเป็นระเบียบ แก้ไขง่าย รองรับการขยายตัว และที่สำคัญ **ปลอดภัยและประหยัดงบที่สุด**

## 🌟 1. หลักการหัวใจสำคัญ: Feature-based Architecture
เราจะจัดเก็บไฟล์ตาม **"หน้าที่การทำงาน" (What it does)** แทนที่จะเก็บตาม **"ประเภทไฟล์" (What it is)**

---

## 📂 2. คู่มือการวางไฟล์ (Folder Structure)

### `src/features/` (🧠 สมองของระบบ)
**เก็บ Logic ธุรกิจ, การคำนวณ, และการติดต่อ Database (API)**
โฟลเดอร์ในนี้จะแยกตาม "ฟีเจอร์" ของระบบ
- **ตัวอย่าง**: `src/features/attendance/`, `src/features/people/`
- **กฎเหล็ก**: ถ้าเป็นเรื่องข้อมูล, การคำนวณเงิน, หรือติดต่อ Server **ต้องอยู่ที่นี่เท่านั้น**

### `src/components/` (🎨 หน้าตา UI)
**เก็บชิ้นส่วนหน้าจอ และ Widget ต่างๆ**
เน้นเรื่องความสวยงาม การแสดงผล โดยมี Logic ให้น้อยที่สุด (รับค่า Props มาแสดง)
- **กฎเหล็ก**: ถ้าเป็นสิ่งที่ "ตามองเห็น" หรือ "กดได้" แต่ไม่ได้ถือข้อมูลเอง ให้เก็บที่นี่

### `src/pages/` (🧩 คนประกอบร่าง)
**หน้าจอหลักที่ผู้ใช้งานเข้าถึง (ผ่าน URL)**
- **หน้าที่**:
  1. เรียก **Hook** จาก `features/` เพื่อขอข้อมูล
  2. นำข้อมูลไปใส่ใน **Component** จาก `components/`
- **กฎเหล็ก**: ❌ **ห้าม** เขียนฟังก์ชันคำนวณยาวๆ หรือเรียก Firebase โดยตรงในหน้านี้เด็ดขาด!

---

## 🛑 3. ข้อควรทำ และ ข้อห้าม (Dos & Don'ts)

| ✅ ควรทำ (Do) | ❌ ห้ามทำ (Don't) |
| :--- | :--- |
| **สร้าง Hook ใหม่** ใน `features/` เมื่อมี Logic ซับซ้อน | เขียน `useEffect` ดึง Database ยาวๆ ในไฟล์ `Page.jsx` |
| **แยก Component** เมื่อ UI เริ่มยาวเกินไป | เขียน HTML/JSX ยาว 200 บรรทัดในไฟล์เดียว |
| **ให้ Page เป็นแค่คนประสานงาน** | ให้ Page ถือ State และ Logic ทุกอย่างไว้คนเดียว |

---

## 🔥 4. Firestore Zero-Cost Strategy (คัมภีร์ใช้งานฟรีตลอดไป)

**หลักการ: "อ่านให้น้อยที่สุด เขียนเท่าที่จำเป็น"**
Firestore คิดเงินตามจำนวน Read/Write ดังนั้นเราต้องออกแบบ Data Structure ให้ฉลาด

### 4.1 Denormalization (เก็บข้อมูลซ้ำ ดีกว่าอ่านหลายรอบ)
❌ **ห้าม**: เก็บแค่ `userId` ในตารางเข้างาน แล้วต้องไป Query ตาราง Users อีกทีเพื่อดึงชื่อมาแสดง
✅ **ต้องทำ**: เก็บ `userId`, `userName`, `userAvatar` ลงไปในตาราง Attendance เลย
> *ยอมเปลืองพื้นที่เก็บ (Storage ถูกมาก) เพื่อแลกกับการไม่ต้อง Read ซ้ำซ้อน (Read แพงกว่า)*

### 4.2 Aggregation Counters (ห้าม Count Runtime)
❌ **ห้าม**: `getDocs` พนักงานทั้งหมดมาเพื่อ `snap.size` หาจำนวนพนักงาน (เปลือง Read = N)
✅ **ต้องทำ**: สร้าง Field `totalEmployees` ไว้ใน collection `companies`
- เมื่อเพิ่มพนักงาน -> +1 ใน `companies`
- เมื่อลบพนักงาน -> -1 ใน `companies`
> *อ่านค่าแค่ 1 Document ก็ได้ตัวเลขเลย ประหยัดไปมหาศาล*

### 4.3 No-Join Policy (ห้าม Join ฝั่ง Client)
❌ **ห้าม**: ดึง A มาก่อน แล้ววนลูปดึง B ตาม A
✅ **ต้องทำ**: ออกแบบให้จบใน Query เดียว หรือใช้ `snapshot` listeners ที่ฉลาด

---

## 🛡️ 5. Security & Privacy (ความลับลูกค้าคือพระเจ้า)

**แอป HRM ข้อมูลหลุดคือหายนะ** ห้ามให้ข้อมูลบริษัท A ไปโผล่ที่บริษัท B เด็ดขาด

### 5.1 Company Isolation (กฎเหล็กบรรทัดแรก)
ทุก Query ที่ออกจากแอป **ต้องมีเงื่อนไขนี้เสมอ**:
```javascript
where("companyId", "==", currentUser.companyId)
```
❌ ห้ามลืมเด็ดขาด! แม้จะเป็น Admin ก็เห็นได้แค่บริษัทตัวเอง

### 5.2 Context Trust (อย่าเชื่อ Client input)
❌ **ห้าม**: ส่ง `companyId` มาจาก URL params หรือ Hidden Input ใน Form (User แก้เองได้)
✅ **ต้องทำ**: ดึง `companyId` จาก `AuthContext` หรือ `currentUser` object ที่ผ่านการ verify token มาแล้วเท่านั้น

### 5.3 Role-Based Access Control (RBAC)
การซ่อนปุ่ม **ไม่ใช่** Security!
- ในไฟล์ `features/` ต้องเช็ค `currentUser.role` ก่อนทำคำสั่งสำคัญเสมอ
- เช่น `deleteEmployee` ต้องเช็คว่าคนเรียกเป็น `admin` หรือ `owner` จริงๆ

---
*Verified & Standardized: February 2026*
